export const setUser = (user) => ({
  type: "SET_USER",
  user,
});

export const setRooms = (rooms) => ({
  type: "SET_ROOMS",
  rooms,
});

export const addRoom = (room) => ({
  type: "ADD_ROOM",
  room,
});
